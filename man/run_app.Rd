% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_app.R
\name{run_app}
\alias{run_app}
\title{Launch the Country Risk Explorer Shiny Application}
\usage{
run_app(raw_data, radar_data, corrected_radar_data, decomposition = NULL)
}
\arguments{
\item{raw_data}{A data frame containing the full dataset with country-level
health and risk indicators. This dataset is used across all visualizations.}

\item{radar_data}{A data frame containing a subset of the raw data
specifically
formatted for radar chart visualization. It should include numeric indicators
relevant to health risk dimensions.}

\item{corrected_radar_data}{A data frame similar in structure to \code{radar_data},
but with severity-adjusted values used to compare original and corrected
risk profiles.}

\item{decomposition}{Precomputed output of \code{\link[=build_decomposition]{build_decomposition()}}. When
\code{NULL} the decomposition tab reports that it is unavailable rather than
failing.}
}
\value{
A \code{shiny.appobj}. Printing it (which is what happens when it is
returned at the console) or passing it to \code{\link[shiny:runApp]{shiny::runApp()}} launches the
app in the user's default web browser.
}
\description{
This function initializes and launches an interactive Shiny web application
designed to explore and visualize country-level health and crisis risk data.
The app includes a score decomposition (waterfall and contribution bars),
a 3D scatterplot, a radar chart, a risk distribution histogram, and an
adjusted risk map.
}
\details{
The decomposition is \strong{precomputed} by the \code{risk_decomposition} target
rather than calculated here: the Shapley values behind it are the expensive
part, and recomputing them on every country selection would make the app
unusable.

The returned app object is self-contained: it carries its own copy of the
plotting helpers, so it still runs after being restored with
\code{targets::tar_read(shiny_explorer)} in a session where \code{tar_source()} has not
been called. See \code{self_contained()}.
}
