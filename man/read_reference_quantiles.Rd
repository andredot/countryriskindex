% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{read_reference_quantiles}
\alias{read_reference_quantiles}
\title{Locate and read the frozen reference quantile table}
\usage{
read_reference_quantiles(file = "reference_quantiles_2025.csv")
}
\arguments{
\item{file}{Name of the reference file within \code{inst/extdata}.}
}
\value{
A data frame with columns \code{variable}, \code{q01}, \code{q99}, \code{n_obs}. Zero
rows when no reference is available.
}
\description{
Looks for the reference file in the installed package and, failing that, in
the source tree (so \code{devtools::load_all()} works before installation).
}
\details{
A missing or empty file is \strong{not} an error: the pipeline degrades to
run-relative normalisation with a warning, so a fresh checkout still runs and
can generate the baseline. It does mean the resulting scores are not
comparable with previous releases, which is what the warning says.

Note that this is deliberately not a \code{targets} \code{format = "file"} target:
after regenerating the reference, run
\code{targets::tar_invalidate(reference_quantiles)} to force a rebuild.
}
