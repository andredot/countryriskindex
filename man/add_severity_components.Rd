% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/severity_.R
\name{add_severity_components}
\alias{add_severity_components}
\title{Apply crisis severity to the individual risk components}
\usage{
add_severity_components(
  df,
  cm_data = NULL,
  betas = severity_betas(),
  channel_map = default_channel_map(),
  sources = default_severity_sources(),
  kappa = 2,
  w_floor = 0.25
)
}
\arguments{
\item{df}{A data frame containing \code{hazard_score}, \code{vulnerability_score},
\code{capacity_score}, the severity sub-dimensions and the one-hot driver
columns.}

\item{cm_data}{The crisis modifier matrix. If \code{NULL}, channel weights fall
back to \code{w_floor} for every country with a severity record.}

\item{betas}{Named numeric vector of log-odds coefficients.}

\item{channel_map}{Named list mapping components to channel columns.}

\item{sources}{Named character vector mapping components to severity columns.}

\item{kappa}{Saturation constant for driver counts.}

\item{w_floor}{Minimum channel weight where a crisis exists but no driver
matched.}
}
\value{
The input data frame with added columns: \verb{w_*}, \verb{n_drivers_*},
\code{driver_matched}, \verb{i_*}, \verb{delta_*} (the log-odds shift actually applied)
and \code{hazard_score_adj}, \code{vulnerability_score_adj}, \code{capacity_score_adj}.
}
\description{
Routes each INFORM Severity sub-dimension onto the risk component it acts on
and applies a bounded log-odds shift:
}
\details{
\deqn{component_{adj} = \mathrm{logistic}\left(\mathrm{logit}(component) +
  \beta_k \, w_k \, i_k\right)}

where \eqn{w_k} is the driver-derived channel weight
(\code{\link[=severity_channel_weights]{severity_channel_weights()}}), \eqn{i_k} the severity intensity
(\code{\link[=severity_intensities]{severity_intensities()}}) and \eqn{\beta_k} the policy coefficient
(\code{\link[=severity_betas]{severity_betas()}}).

The adjustment is bounded (a component can never reach 1), monotone in
severity, and expressed as a log odds ratio so it is interpretable and
comparable across baselines.
}
