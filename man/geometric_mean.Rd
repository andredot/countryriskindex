% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/small_utils.R, R/utils.R
\name{geometric_mean}
\alias{geometric_mean}
\title{Compute the geometric mean}
\usage{
geometric_mean(x, min_n = 1, eps = 1e-04)

geometric_mean(x, min_n = 1, eps = 1e-04)
}
\arguments{
\item{x}{Numeric vector.}

\item{min_n}{Minimum number of non-missing values required; below this the
function returns \code{NA_real_}. Defaults to 1 (previous behaviour).}

\item{eps}{Small positive value used to clamp non-positive inputs.}
}
\value{
Geometric mean as a single numeric value.

Geometric mean as a single numeric value.
}
\description{
Calculates the geometric mean of a numeric vector, ignoring NAs.

Calculates the geometric mean of a numeric vector, ignoring \code{NA}s.
}
\details{
Two guards are applied that the naive \code{exp(mean(log(x)))} form lacks:
non-positive values are clamped to \code{eps} (otherwise a single zero collapses
the whole aggregate to zero, which happens routinely after quantile
normalisation), and the result is \code{NA} when fewer than \code{min_n} non-missing
values are available (otherwise a country measured on two indicators is
silently treated as equivalent to one measured on four).
}
\examples{
geometric_mean(c(1, 4, 16))
geometric_mean(c(1, NA, NA), min_n = 2)
}
