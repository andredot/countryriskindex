% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_.R
\name{run_pca_validation}
\alias{run_pca_validation}
\title{Run a PCA on the index components}
\usage{
run_pca_validation(df, vars = default_pca_vars()$indicators, scale. = TRUE)
}
\arguments{
\item{df}{A data frame containing the variables in \code{vars}.}

\item{vars}{Character vector of variable names. Defaults to the sub-component
indicators from \code{\link[=default_pca_vars]{default_pca_vars()}}.}

\item{scale.}{Logical: scale variables to unit variance before analysis.
Should normally stay \code{TRUE} since the inputs are on different scales.}
}
\value{
A list with elements \code{pca} (the \code{prcomp} object), \code{variance} (a data
frame of eigenvalues and explained variance), \code{loadings} (a long data frame
of variable loadings), \code{scores} (observation scores with country labels),
\code{n_complete} and \code{vars}.
}
\description{
Performs a principal component analysis on the scaled, complete-case subset
of \code{vars}, and summarises how much of the total variance the leading
components explain.
}
