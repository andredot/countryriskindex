% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_.R
\name{add_data_completeness}
\alias{add_data_completeness}
\title{Flag countries with insufficient underlying data}
\usage{
add_data_completeness(
  df,
  indicator_sets = default_indicator_sets(),
  max_missing = 3,
  max_missing_component = 0.5
)
}
\arguments{
\item{df}{A data frame of merged indicators.}

\item{indicator_sets}{Named list of indicator names per component. Defaults
to \code{\link[=default_indicator_sets]{default_indicator_sets()}}.}

\item{max_missing}{Maximum number of missing indicators tolerated before a
country is flagged as low confidence.}

\item{max_missing_component}{Maximum proportion of a single component's
indicators that may be missing before the country is flagged, regardless of
the overall count.}
}
\value{
The input data frame with added columns \verb{n_missing_<component>},
\code{n_missing_total}, \code{data_completeness} (proportion present in \verb{[0, 1]}) and
\code{low_confidence} (logical).
}
\description{
Counts missing input indicators per component and flags countries whose score
rests on too thin an evidence base.
}
\details{
This matters because data sparsity correlates with fragility: without an
explicit flag, the countries most likely to be scored from a partial variable
set are exactly the high-risk ones, and their scores are silently not
comparable with those of well-measured countries.
}
