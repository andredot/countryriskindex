% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess_.R
\name{collapse_severity_by_country}
\alias{collapse_severity_by_country}
\title{Collapse multiple crises to one row per country}
\usage{
collapse_severity_by_country(df)
}
\arguments{
\item{df}{A data frame from \code{\link[=preprocess_severity]{preprocess_severity()}}, one row per crisis.}
}
\value{
A data frame with one row per \code{ISO3}, plus \code{n_crises} and a
semicolon-separated \code{CRISIS} listing the crises that were merged.
}
\description{
INFORM Severity records one row per \emph{crisis}, and a country may host several.
Left-joining that directly onto the country table duplicates rows silently.
This function reduces to one row per ISO3 code: severity dimensions take the
country's \strong{maximum} (the binding constraint is the worst concurrent
crisis, not their average), and the one-hot driver flags take the union, so
that a country with two crises is credited with the drivers of both.
}
