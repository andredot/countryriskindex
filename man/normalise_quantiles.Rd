% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/small_utils.R, R/utils.R
\name{normalise_quantiles}
\alias{normalise_quantiles}
\title{Normalize values between the 1st and 99th percentiles}
\usage{
normalise_quantiles(x, q01 = NULL, q99 = NULL)

normalise_quantiles(x, q01 = NULL, q99 = NULL)
}
\arguments{
\item{x}{Numeric vector.}

\item{q01}{Optional numeric scalar: frozen lower (1st percentile) bound.}

\item{q99}{Optional numeric scalar: frozen upper (99th percentile) bound.}
}
\value{
A normalized numeric vector.

A numeric vector scaled to \verb{[0, 1]}.
}
\description{
Scales a numeric vector so that the 1st percentile becomes 0 and the 99th becomes 1.

Scales a numeric vector so that the reference 1st percentile maps to 0 and
the reference 99th percentile maps to 1, clipping outside that range.
}
\details{
By default the reference percentiles are computed from \code{x} itself, which
makes the resulting score \emph{relative to the current run}: a country's value
changes when other countries change, even if its own inputs are identical.
For year-on-year comparability, pass frozen bounds via \code{q01}/\code{q99} (see
\code{\link[=compute_reference_quantiles]{compute_reference_quantiles()}} and \code{\link[=get_reference_bounds]{get_reference_bounds()}}).
}
\examples{
normalise_quantiles(c(1, 5, 10))
normalise_quantiles(c(1, 5, 10), q01 = 0, q99 = 20)
}
