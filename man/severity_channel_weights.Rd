% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/severity_.R
\name{severity_channel_weights}
\alias{severity_channel_weights}
\title{Compute per-country severity channel weights from crisis drivers}
\usage{
severity_channel_weights(
  df,
  cm_data = NULL,
  channel_map = default_channel_map(),
  kappa = 2,
  w_floor = 0.25
)
}
\arguments{
\item{df}{A data frame with one row per country, containing the one-hot
\verb{Driver *} columns produced by \code{\link[=preprocess_severity]{preprocess_severity()}} and a
\code{severity_index} column.}

\item{cm_data}{The crisis modifier matrix: a data frame with a \code{Driver}
column and one logical column per channel.}

\item{channel_map}{Named list mapping components to channel column names.
Defaults to \code{\link[=default_channel_map]{default_channel_map()}}.}

\item{kappa}{Saturation constant; higher values mean slower saturation.}

\item{w_floor}{Minimum weight applied to countries that have a severity
record but no matched driver.}
}
\value{
A data frame with columns \code{w_hazard}, \code{w_vulnerability},
\code{w_capacity}, the driver counts \verb{n_drivers_*}, and a logical
\code{driver_matched} diagnostic.
}
\description{
For each risk component, counts how many of the country's active INFORM
Severity drivers act on that component according to the crisis modifier
matrix, then converts the count to a saturating weight in \verb{[0, 1)}:
}
\details{
\deqn{w = n / (n + \kappa)}

so that one driver gives 0.33, two give 0.50 and four give 0.67 (at the
default \code{kappa = 2}). Saturation prevents countries with long driver lists
from accumulating unbounded uplift.

Countries that have a severity record but no driver matching a channel
receive \code{w_floor} rather than zero, so that a coding gap in the driver matrix
cannot silently switch the crisis modifier off.
}
