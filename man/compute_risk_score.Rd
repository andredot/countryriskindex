% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculator_.R
\name{compute_risk_score}
\alias{compute_risk_score}
\title{Compute the risk score from a set of input files}
\usage{
compute_risk_score(
  paths,
  inform_risk_sheet = 2,
  inform_lcc_sheet = 5,
  reference_quantiles = read_reference_quantiles(),
  progress = NULL
)
}
\arguments{
\item{paths}{Named character vector or list of file paths, with the ids of
\code{\link[=risk_input_files]{risk_input_files()}} as names.}

\item{inform_risk_sheet, inform_lcc_sheet}{Sheets of the INFORM Risk workbook
holding the risk indicators and the Lack of Coping Capacity indicators.}

\item{reference_quantiles}{Output of \code{\link[=read_reference_quantiles]{read_reference_quantiles()}}.}

\item{progress}{Optional \verb{function(step, i, n)} called before each step.}
}
\value{
A list with \code{risk_score} (the scored data frame) and \code{log} (a data
frame with one row per step, its duration and the number of rows it
produced).
}
\description{
Runs the same import, preprocessing and scoring steps as the \code{risk_score}
target, but on explicitly supplied files. Each step is labelled, so an error
says which input it came from (typically: the wrong file was selected).
}
