% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/severity_.R
\name{add_severity}
\alias{add_severity}
\title{Compute the severity-adjusted risk score}
\usage{
add_severity(
  df,
  cm_data = NULL,
  method = c("components", "convex"),
  min_n = 3,
  ...
)
}
\arguments{
\item{df}{A data frame with component scores and severity data.}

\item{cm_data}{The crisis modifier matrix.}

\item{method}{Either \code{"components"} (default, driver-routed log-odds) or
\code{"convex"} (deprecated v4.0 formula).}

\item{min_n}{Minimum number of non-missing components required, passed to
\code{\link[=row_geometric_mean]{row_geometric_mean()}}.}

\item{...}{Further arguments passed to \code{\link[=add_severity_components]{add_severity_components()}}.}
}
\value{
The input data frame with added columns \code{severity_adjusted_risk} and
\code{severity_uplift}, plus the component diagnostics.
}
\description{
Combines the severity-adjusted components into the headline score using the
same geometric mean as the unadjusted \code{\link[=add_overall_risk]{add_overall_risk()}}, so that the two
are directly comparable and their difference (\code{severity_uplift}) is
attributable entirely to the crisis modifier.
}
\details{
The legacy v4.0 behaviour is retained under \code{method = "convex"} purely so
that the change can be quantified; it should not be used for reporting.
}
