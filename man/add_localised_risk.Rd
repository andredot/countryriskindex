% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_.R
\name{add_localised_risk}
\alias{add_localised_risk}
\title{Add localised risk to country-level risk data}
\usage{
add_localised_risk(df, proximity_adjustments)
}
\arguments{
\item{df}{A tibble with one row per country, including \code{ISO_A3} and \code{severity_adjusted_risk}}

\item{proximity_adjustments}{A tibble with columns: ISO_A3, disease_score, outbreak_score, people_score, final_score}
}
\value{
A tibble with all original columns plus \code{localised_risk}
}
\description{
This function merges proximity adjustment scores with a country-level risk table
and computes a new column \code{localised_risk} using an odds-based adjustment.
}
\examples{
df <- tibble::tibble(
  ISO_A3 = c("NGA", "TUV", "UGA"),
  severity_adjusted_risk = c(0.5, 0.4, 0.6)
)

proximity_adjustments <- tibble::tibble(
  ISO_A3 = c("NGA", "TUV", "UGA"),
  disease_score = c(0.3, 0.3, 0.3),
  outbreak_score = c(0.0892, 0.0892, 0.0892),
  people_score = c(0.301, 0.301, 0.301),
  final_score = c(0.690, 0.690, 0.690)
)

add_localised_risk(df, proximity_adjustments)
}
