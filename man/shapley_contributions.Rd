% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explain_.R
\name{shapley_contributions}
\alias{shapley_contributions}
\title{Exact Shapley values for a scalar function of a few inputs}
\usage{
shapley_contributions(f, x_country, x_ref)
}
\arguments{
\item{f}{A function taking a named numeric vector and returning one number.}

\item{x_country}{Named numeric vector of the country's values.}

\item{x_ref}{Named numeric vector of reference values, same names and order.}
}
\value{
A named numeric vector of contributions summing to
\code{f(x_country) - f(x_ref)}.
}
\description{
Enumerates all \code{2^n} coalitions, so this is exact rather than sampled. Keep
\code{n} small: the index uses it per pillar (four inputs, sixteen coalitions),
never over all indicators at once.
}
\examples{
f <- function(x) log(prod(x))
shapley_contributions(f, c(a = 2, b = 8), c(a = 1, b = 1))
}
