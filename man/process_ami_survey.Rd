% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{process_ami_survey}
\alias{process_ami_survey}
\title{Process AMI survey responses enforcing monotonic concern by group}
\usage{
process_ami_survey(
  data,
  diag_cols = 1:4,
  transport_cols = 5:8,
  impact_cols = 9:10,
  concern_levels = c("Very unconcerned", "Somewhat unconcerned",
    "Neither concerned nor unconcerned", "Somewhat concerned", "Very concerned"),
  treat_not_relevant_as_na = TRUE
)
}
\arguments{
\item{data}{A data frame containing the 10 AMI columns (in any order).}

\item{diag_cols}{Columns (names or positions) for the diagnosis delay group (length 4).
Default: first 4 columns.}

\item{transport_cols}{Columns (names or positions) for the transport delay group (length 4).
Default: next 4 columns.}

\item{impact_cols}{Columns (names or positions) for health/wellbeing (length 2).
Default: last 2 columns.}

\item{concern_levels}{Character vector of ordered concern levels (low → high).}

\item{treat_not_relevant_as_na}{Logical; turn strings with "Not relevant" into \code{NA} (default TRUE).}
}
\value{
A data frame with the same columns, all as ordered factors with uniform levels,
and with monotonic constraints enforced within the first two groups.
}
\description{
Enforces non-decreasing concern within two groups:
(1) diagnosis delay (first 4 columns) and (2) transport delay (next 4 columns).
The last two columns (health/wellbeing) are independent.

Within each of the first two groups and within each row, scanning left-to-right,
any entry lower than the highest seen so far is raised to that highest. \code{NA}s are
treated the same (filled with the highest seen so far). Leading \code{NA}s remain \code{NA}
until a level appears later in that group.

"Not relevant for this condition" (case-insensitive) is converted to \code{NA} by default.
All columns are returned as ordered factors with the same \code{concern_levels}.
}
