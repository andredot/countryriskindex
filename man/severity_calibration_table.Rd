% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/severity_.R
\name{severity_calibration_table}
\alias{severity_calibration_table}
\title{Tabulate the effect of the severity adjustment across scenarios}
\usage{
severity_calibration_table(
  baseline = c(0.2, 0.35, 0.5, 0.65),
  severity = c(1, 2, 3, 4, 5),
  n_drivers = c(0, 1, 2, 4, 6),
  betas = severity_betas(),
  kappa = 2,
  w_floor = 0.25
)
}
\arguments{
\item{baseline}{Numeric vector of baseline component scores to evaluate. Each
value is used for all three components, so the unadjusted geometric mean
equals the baseline itself.}

\item{severity}{Numeric vector of INFORM Severity index values (1-5).}

\item{n_drivers}{Integer vector of active driver counts per channel.}

\item{betas}{Named numeric vector of log-odds coefficients.}

\item{kappa}{Saturation constant for driver counts.}

\item{w_floor}{Minimum channel weight.}
}
\value{
A data frame with one row per scenario and columns \code{baseline},
\code{severity}, \code{n_drivers}, \code{weight}, \code{risk_v4_1}, \code{risk_v4_0},
\code{uplift_v4_1}, \code{uplift_v4_0}.
}
\description{
Produces the calibration table used to justify the choice of
\code{\link[=severity_betas]{severity_betas()}}. For a grid of baseline risks, severity indices and driver
counts it reports the adjusted score under both the v4.1 driver-routed
log-odds method and the deprecated v4.0 convex formula, so the change in
behaviour is auditable rather than asserted.
}
\examples{
severity_calibration_table(baseline = 0.35, severity = c(1, 3, 5),
                           n_drivers = c(0, 2, 6))
}
