% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import_.R
\name{pick_column}
\alias{pick_column}
\title{Pick a column by name, disambiguating duplicates by expected range}
\usage{
pick_column(df, prefix, range = NULL, required = TRUE)
}
\arguments{
\item{df}{A data frame.}

\item{prefix}{The column name, or the stem of a positionally-suffixed name.}

\item{range}{Numeric length-2 vector giving the plausible \code{c(min, max)}.
Strongly recommended: it is what makes the selection stable.}

\item{required}{Whether a missing column is an error (\code{TRUE}) or returns
\code{NA} (\code{FALSE}).}
}
\value{
A numeric vector.
}
\description{
\code{readxl} disambiguates duplicated headers positionally, producing names like
\verb{Road density...15} and \verb{Road density...16}. Selecting by positional suffix
is fragile: if the source workbook gains or loses a column, a different
variable is silently picked up and the index changes for reasons nobody can
trace.
}
\details{
The INFORM Lack of Coping Capacity sheet carries road density twice - once as
the raw value (roughly 1 to 850 km per unit area) and once as the normalised
INFORM 0-10 score. The two are cleanly separable by range, so this function
selects the candidate that \emph{satisfies the contract} rather than the first or
last one. That is stable across workbook revisions in a way that a position
is not.

Resolution order:
\enumerate{
\item Collect columns whose name is \code{prefix} or \code{prefix...N}.
\item If \code{range} is supplied, keep only candidates whose observed values
fall inside it.
\item Exactly one survivor: use it. None: error, reporting each
candidate's observed range. Several: prefer an exact name match, and
otherwise error as ambiguous.
}
}
\examples{
d <- data.frame(
  `Road density...15` = c(1.5, 844),   # raw
  `Road density...16` = c(0.2, 9.8),   # INFORM 0-10 score
  check.names = FALSE
)
pick_column(d, "Road density", range = c(0, 10))
}
