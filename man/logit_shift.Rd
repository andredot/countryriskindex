% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{logit_shift}
\alias{logit_shift}
\title{Shift a probability on the log-odds scale}
\usage{
logit_shift(p, delta, eps = 0.001)
}
\arguments{
\item{p}{Numeric vector of probabilities in \verb{[0, 1]}.}

\item{delta}{Numeric vector of log-odds shifts (recycled against \code{p}).}

\item{eps}{Clamp applied to \code{p} before transformation, to keep \code{qlogis()}
finite.}
}
\value{
A numeric vector of adjusted probabilities.
}
\description{
Applies \code{plogis(qlogis(p) + delta)}. Unlike an additive shift in probability
space, this is bounded (never reaches 0 or 1), monotone, and has a constant
odds-ratio interpretation: \code{delta} is a log odds ratio regardless of the
baseline. It is the transformation used for every adjustment in this package
so that all modifiers are expressed on the same scale.
}
\examples{
logit_shift(0.35, 1.2)
logit_shift(c(0.2, 0.5, 0.9), 0.5)
}
